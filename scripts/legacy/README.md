# Scripts hérités

Fichiers déplacés ici faute de référence dans `package.json` ou la documentation active du pipeline. À réutiliser uniquement si tu en as besoin ponctuellement ; le flux SITADEL courant passe par les scripts `enrich-sitadel-*.mjs`.

- **`import-sitadel-neon.py`** — import Sitadel vers Postgres (ancien flux).
- **`analyze-osm-buildings.py`** — analyse d’un extrait OSM `.osm.pbf`.
